---
to: src/client/reducers/<%= name %>.js
---
const initialState = {
};

export default (state, action) => {
    if (!state) {
        state = initialState;
    }

    switch (action.type) {
    }

    return state;
}
